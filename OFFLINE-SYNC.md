# Offline First — สรุปสิ่งที่เพิ่มเข้ามา

ไม่มีการแก้ไขระบบ Login เดิม (`useAuth.ts`, `useProfile.ts`, การกรอกโปรไฟล์)
และไม่มีการแก้ UI หลัก (Home, Map, BottomNav ฯลฯ) — ของใหม่ทั้งหมดอยู่ในไฟล์ต่อไปนี้

## ไฟล์ใหม่

- **`composables/useOfflineSync.ts`** — หัวใจของระบบ Offline First
  - เก็บ "คิว" ฐานที่ผ่านสำเร็จแต่ยังไม่ได้ Sync ไว้ที่ LocalStorage
    (key: `offlineSync:pendingCheckins`)
  - `queueCheckin(station, point)` — เพิ่มฐานเข้าคิว (กันซ้ำในตัว)
  - `syncNow()` — ทำตามลำดับ: **Login LINE** (ถ้ายังไม่ Login, เรียกฟังก์ชันเดิม
    จาก `useAuth.ts` ตรง ๆ ไม่ได้แก้ไข) → **Sync Members** (เผื่อกรณียังไม่มี
    `memberId`) → **Sync Journey** (ยิง action `checkin` ทีละฐานที่ค้างอยู่) →
    **Sync Score** (ดึงคะแนนล่าสุดจาก Google Sheet กลับมาอัปเดตในเครื่อง)
  - ถ้าไม่มีอินเทอร์เน็ต จะคืนค่า `reason: 'offline'` ทันที ไม่แตะคิวเลย

- **`plugins/offline-sync.client.ts`** — ผูก `window.addEventListener('online'/'offline')`
  ไว้ที่เดียวทั้งแอป เมื่อกลับมาออนไลน์และมีข้อมูลค้างอยู่ในคิว จะสั่ง Sync
  ให้อัตโนมัติทันที (ตรงกับข้อความ "ข้อมูลจะ Sync อัตโนมัติเมื่อมีอินเทอร์เน็ต")

## ไฟล์ที่แก้ไข (แบบเพิ่มเติมเท่านั้น ไม่ลบของเดิม)

- **`composables/useMemberApi.ts`** — เพิ่มฟังก์ชัน `checkin()`, `getJourney()`,
  `getScore()` ที่ห่อ action `checkin` / `getJourney` / `getScore` ซึ่งมีอยู่แล้ว
  ในฝั่ง backend (`server-gas/CheckinService.gs`) แต่ frontend ยังไม่เคยเรียกใช้
  ฟังก์ชันเดิมทุกตัว (`checkMember`, `register`, `login`, `loginByLine`,
  `updateMember`, `getMember`, `syncMember`) **ไม่ถูกแก้ไข**

- **`pages/scan.vue`** — เพิ่ม Logic เกม (ไม่แก้ Logic กล้อง/QR เดิม):
  - ช่องกรอกรหัสฐานเอง รองรับ `CORN001` / `COW001` / `SOIL001` / `MILK001`
    (พิมพ์เล็ก/ใหญ่ไม่สำคัญ) และ QR ที่เข้ารหัสเป็น `corn` / `cow` / `soil` / `milk` ตรง ๆ
  - เมื่อสแกน/กรอกรหัสสำเร็จ: เช็คซ้ำก่อนด้วย `useAdventure().isVisited()` —
    ถ้าเคยผ่านแล้ว **ไม่บันทึกซ้ำ ไม่ Sync** ถ้ายังไม่เคย จะ `toggleStation()`
    (ของเดิมใน `useAdventure.ts`, บันทึกลง LocalStorage + อัปเดต Point/Journey/
    Polyline ให้อัตโนมัติเพราะ Map/Home อ่านจาก state เดียวกัน) แล้วเข้าคิว Sync
  - Sync ขึ้น Google Sheet จะเกิดขึ้นจริงแค่ 2 จังหวะ: ผ่านฐาน **"นม"** (ฐานสุดท้าย)
    หรือกดปุ่ม **"Sync ตอนนี้"** เอง และต้องมีอินเทอร์เน็ตเท่านั้น
  - มีการ์ดสถานะออนไลน์/ออฟไลน์ + จำนวนฐานที่รอ Sync ในหน้านี้

## พร้อมเชื่อม Google Sheet ในอนาคต

Backend (`server-gas/`) มี action `checkin` / `getJourney` / `getScore` /
`getLeaderboard` พร้อมใช้งานอยู่แล้ว (กันบันทึกซ้ำฝั่ง server อีกชั้นด้วย) —
เพียงตั้งค่า `API_BASE_URL` ใน `.env` ให้ถูกต้องตาม `server-gas/README.md`
ระบบ Sync ก็จะทำงานได้ทันทีโดยไม่ต้องแก้โค้ด Frontend เพิ่มอีก

## อัปเดต (รอบตรวจสอบ Offline First เพิ่มเติม)

ระบบเดิม (ด้านบน) ครอบคลุมสเปกส่วนใหญ่อยู่แล้ว: บันทึกลง LocalStorage ก่อนเสมอ,
กันข้อมูลซ้ำ, retry เมื่อกลับมาออนไลน์, และหน้า Home/History/Map ก็อ่านจาก
LocalStorage ล้วน ๆ อยู่แล้ว (ไม่พึ่ง API ตรง ๆ) ตรวจสอบแล้วเจอ 2 จุดที่ควรเสริม
ให้ตรงสเปกครบถ้วนขึ้น โดยแก้แบบ "เพิ่มเติมเท่านั้น" เช่นเดิม:

1. **`plugins/offline-sync.client.ts`** — เดิม Sync อัตโนมัติจะเกิดเฉพาะตอน
   "กลับมาออนไลน์ระหว่างใช้งาน" (`window.addEventListener('online', ...)`)
   เท่านั้น ถ้าปิดแอประหว่างออฟไลน์ (มีคิวค้างอยู่) แล้วเปิดแอปใหม่ตอนที่ "มีเน็ต
   อยู่แล้วตั้งแต่แรก" ระบบเดิมจะไม่ Sync ให้อัตโนมัติ (ต้องรอกดปุ่มเองที่หน้า Scan)
   ซึ่งไม่ตรงกับสเปกข้อ "เปิดแอปใหม่พร้อมมีอินเทอร์เน็ต ให้ Sync อัตโนมัติ" —
   แก้โดยเพิ่มการเรียก Sync แบบเงียบ ๆ ทันทีตอน plugin เริ่มทำงาน (ถ้าออนไลน์อยู่
   แล้วและมีคิวค้าง) และเพิ่ม `setInterval` ทุก 45 วินาทีเป็นตัวกันเหนียว เผื่อกรณี
   Sync ครั้งก่อนล้มเหลวแบบชั่วคราว (เช่น Apps Script ตอบช้า) โดยไม่มี event
   'online'/'offline' มาช่วยกระตุ้นให้ลองใหม่ — ทั้งหมดยังเรียกผ่าน `syncNow()`
   ตัวเดิม ซึ่งกันการ Sync ซ้อนกันเองอยู่แล้วด้วย `isSyncing`

2. **`composables/useOfflineSync.ts`** + **`composables/useMemberApi.ts`** —
   เพิ่มฟิลด์ `uuid` ให้แต่ละรายการใน Queue (`PendingCheckin.uuid`, สร้างด้วย
   `crypto.randomUUID()` มี fallback ให้เบราว์เซอร์เก่า) แล้วส่งแนบไปกับ
   `checkin()` เป็น `clientId` (ฟิลด์ใหม่ที่เป็น optional ใน `CheckinPayload`)
   เพื่อให้มี "รหัสอ้างอิงเฉพาะ" ต่อ 1 เหตุการณ์ตามสเปก (เดิมกันข้อมูลซ้ำด้วย
   `stationId` ต่อผู้เล่น 1 คนอยู่แล้วซึ่งเพียงพอสำหรับเกมนี้ — `clientId` เป็น
   ข้อมูลเสริมไว้ตรวจสอบย้อนหลัง/debug ไม่ได้เปลี่ยน logic การกันซ้ำเดิม)
   ฝั่ง `server-gas/CheckinService.gs` ไม่ต้องแก้อะไรเลย เพราะรับ payload
   เป็น object แล้วดึงเฉพาะฟิลด์ที่รู้จัก ฟิลด์ใหม่ที่ส่งเพิ่มมาจะถูกเพิกเฉยไปเอง

จุดที่ตรวจสอบแล้วว่า **ไม่ต้องแก้**:

- `composables/useAdventure.ts`, `composables/useProfile.ts` — โหลดจาก
  LocalStorage เป็นอันดับแรกอยู่แล้วผ่าน `initAdventure()` / `initProfile()`
  ก่อนที่จะพยายามคุยกับ Google Sheet ใด ๆ (ตรงกับสเปกข้อ "โหลดจาก LocalStorage
  เป็นอันดับแรกเสมอ")
- `pages/home.vue`, `pages/history.vue` — แสดงผลจาก `useAdventure()` /
  `profile` (LocalStorage) ล้วน ๆ ไม่มีการเรียก API ตรง ๆ ในหน้าเหล่านี้ ใช้งาน
  ได้ปกติแม้ไม่มีเน็ตอยู่แล้ว
- `composables/useRequireProfile.ts` — ตั้ง `isReady = true` จากข้อมูลใน
  LocalStorage ก่อนเสมอ ส่วนการรีเฟรชคะแนนจาก Google Sheet เป็นแค่ส่วนเสริม
  แบบเงียบ ๆ (silent) ที่ห่อด้วย `try/catch` ไม่ block การใช้งานถ้าออฟไลน์
- ระบบ Login/สมัครสมาชิก (`useAuth.ts`, `useProfile.ts`, `ProfileForm.vue`,
  `pages/index.vue`) — **ยังคงต้องมีอินเทอร์เน็ตตอนสมัครสมาชิก/Login ครั้งแรก**
  เหมือนเดิม ไม่ได้แก้ เพราะเป็นขอบเขตที่ระบบเดิมกำหนดไว้ชัดเจนแล้วว่าจะไม่แตะ
  (ดูหัวข้อ "ไฟล์ใหม่" ด้านบน) และงานที่ขอมาครั้งนี้ระบุเฉพาะข้อมูลระหว่างเล่นเกม
  (สแกน QR/คะแนน/ประวัติ) ไม่ได้รวมถึงขั้นตอนสมัครสมาชิก — ถ้าต้องการให้สมัคร
  สมาชิกออฟไลน์ได้ด้วย จะเป็นงานแยกอีกก้อนหนึ่งเพราะกระทบ Flow Login ทั้งหมด
- `composables/useStations.ts`, `composables/useJourney.ts` — โค้ด Mockup เก่า
  ที่ไม่ได้ถูกเรียกใช้จากหน้าไหนในระบบจริงแล้ว (ตรวจสอบด้วย grep ทั้งโปรเจกต์)
  ไม่เกี่ยวข้องกับ Flow Sync จึงไม่แตะ

## อัปเดตรอบที่ 2 — Offline First 100% หลัง Login ครั้งแรก

เป้าหมายรอบนี้: หลังจาก Login + กรอกโปรไฟล์สำเร็จครั้งแรก (ขั้นตอนนี้จำเป็นต้อง
มีอินเทอร์เน็ตอยู่แล้วโดยธรรมชาติ เพราะต้องยืนยันตัวตนกับ LINE และ/หรือขอ
memberId จาก Google Sheet) การเปิดแอปครั้งต่อ ๆ ไปและการเล่นเกมทั้งหมดต้อง
**ไม่ต้องพึ่งอินเทอร์เน็ตเลย** ตรวจสอบทั้งโปรเจกต์อีกรอบ พบ 4 จุดที่เป็นสาเหตุ
ให้แอปยังคงพึ่งอินเทอร์เน็ตอยู่ทั้งที่ไม่จำเป็นแล้ว แก้ไขแบบเพิ่มเติม/ปรับลำดับ
เท่านั้น ไม่ได้เปลี่ยน Logic เดิม:

1. **`pages/index.vue`** (สาเหตุสำคัญที่สุด) — โค้ดเดิมเรียก `await initAuth()`
   (ซึ่งไป `liff.init()` คุยกับเซิร์ฟเวอร์ LINE จริง) แบบไม่มีเงื่อนไข **ก่อน**
   ตรวจสอบ `hasProfile` เสมอ ผลคือทุกครั้งที่เปิดแอปใหม่ — แม้ผู้ใช้จะเคย Login
   สำเร็จและมี `userProfile` ครบใน LocalStorage แล้วก็ตาม — แอปจะพยายามยิง
   Request ไปที่ LINE ก่อนเสมอ ถ้าไม่มีเน็ตจะหน่วงเวลา/ error เงียบ ๆ ก่อนถึงจะ
   เข้าหน้า Home ได้ ไม่ตรงกับเป้าหมาย **แก้โดยสลับลำดับ**: เช็ค `initProfile()`
   (อ่านจาก LocalStorage ล้วน ๆ) ก่อนเสมอ ถ้ามีโปรไฟล์ครบแล้วนำทางไป `/home`
   ทันทีโดยไม่แตะ `initAuth()`/เน็ตเวิร์กเลยสักครั้ง เรียก `initAuth()` เฉพาะตอน
   ที่ยังไม่มีโปรไฟล์เท่านั้น (ซึ่งเป็นสถานการณ์ที่ต้องใช้เน็ตเวิร์กจริง ๆ อยู่แล้ว)

2. **`composables/useRequireProfile.ts`** — Guard ของทุกหน้าหลัง Login (Home,
   Map, Scan, History, Profile, Info, Reservation) เดิมยิง `getMember()` แบบ
   ไม่เช็ค `navigator.onLine` ก่อนเสมอ (แม้จะไม่ block การแสดงผล เพราะตั้ง
   `isReady = true` ก่อนเรียกอยู่แล้ว แต่ยังเป็น Request ที่รู้อยู่แล้วว่าต้อง Fail
   แน่ ๆ ทุกครั้งที่ออฟไลน์) — แก้โดยเพิ่มเงื่อนไข `navigator.onLine` ก่อนเรียก
   ตัดการยิง Request ที่ไม่จำเป็นทิ้งไปเมื่อรู้อยู่แล้วว่าไม่มีเน็ต

3. **`nuxt.config.ts`** — 2 จุด:
   - `nitro.prerender.routes` เดิมมีแค่ `/offline` หน้าเดียว ทำให้หน้าอื่น ๆ
     ทั้งหมด (`/home`, `/map`, `/scan`, `/profile`, `/info`, `/history`,
     `/reservation`, `/`) ไม่ได้ถูก Build เป็นไฟล์ HTML static เลย จึงไม่มีทาง
     ถูก Precache ตอน build ได้ (ต้องพึ่ง NetworkFirst runtime cache ใน
     `service-worker/sw.ts` ที่ทำงานเฉพาะหลังผู้ใช้เคยเข้าหน้านั้นตอนออนไลน์มา
     ก่อนแล้วเท่านั้น) ถ้าผู้ใช้ Login เสร็จแล้วไปหน้า Home แต่ยังไม่เคยกดเข้า
     หน้า Map/Scan/History ฯลฯ เลยสักครั้ง แล้วเน็ตหลุดไปก่อน หน้าพวกนั้นจะเปิด
     ไม่ได้ทันที — แก้โดยเพิ่มทุก Route หลักเข้าไปใน `prerender.routes` ตรวจสอบ
     แล้วว่าทุกหน้าปลอดภัยที่จะ Prerender (ไม่มีการแตะ `window`/`document`/
     LocalStorage ที่ top-level ของ `<script setup>` เลยสักไฟล์ — ทั้งหมดอยู่
     หลัง `import.meta.client` guard หรือถูกเรียกใน `onMounted()`/dynamic
     `import()` เท่านั้น) ทำให้ Service Worker precache ไฟล์ HTML ของทุกหน้า
     ได้ตั้งแต่ตอน build จริง ๆ ไม่ต้องรอผู้ใช้เข้าหน้านั้นก่อนอีกต่อไป
   - `pwa.injectManifest.globPatterns` ไม่มีนามสกุล `webmanifest` อยู่ในลิสต์
     ทำให้ไฟล์ `manifest.webmanifest` ที่ `@vite-pwa/nuxt` สร้างให้อัตโนมัติไม่
     ถูก Precache ไปด้วย — แก้โดยเพิ่มนามสกุลนี้เข้าไป

4. **`service-worker/sw.ts`** — ไม่มีการแก้ Logic ใด ๆ (Cache Strategy เดิม
   ถูกต้องอยู่แล้วทุกประเภทไฟล์: HTML/JS/CSS/Font/Image/API) แค่แก้คอมเมนต์ให้
   ตรงกับข้อ 3 ด้านบน (Precache ตอนนี้ครอบคลุมทุกหน้าแล้ว ไม่ใช่แค่ `/offline`)

จุดที่ตรวจสอบแล้วว่า **ตั้งใจให้ยังต้องใช้อินเทอร์เน็ต** (ตรงกับสเปกที่ระบุว่า
"หลังจากเข้าใช้งานครั้งแรก" เท่านั้นที่ต้อง Offline ได้ 100%):

- **ขั้นตอน Login/สมัครสมาชิกครั้งแรก** (`useAuth.ts` -> `loginWithLine()`,
  `ProfileForm.vue` -> `syncMember()`, `MissingFieldsForm.vue` ->
  `updateMember()`) — ยังคงต้องมีอินเทอร์เน็ต เพราะเป็นการขอ `memberId` จริง
  จาก Google Sheet ครั้งแรก (LINE Login ก็ต้องคุยกับเซิร์ฟเวอร์ LINE โดย
  ธรรมชาติของ OAuth ด้วย) เป็นไปไม่ได้ที่จะสร้าง `memberId` ที่ Sheet ยืนยัน
  แล้วขึ้นมาเองฝั่ง Client โดยไม่มีเน็ตเลย — ทางเลือกเดียวที่ทำได้ถ้าอยากให้
  สมัครสมาชิกแบบออฟไลน์ได้ด้วยคือ "สมัครแบบ Guest" (`loginAsGuest()` ซึ่ง
  ไม่มีการเรียก API เลย ทำงานออฟไลน์ได้ 100% อยู่แล้ว) แต่ขั้นตอนกรอกฟอร์ม
  โปรไฟล์ (`ProfileForm.vue`) ยังคงเรียก `syncMember()` แบบ Block เสมอไม่ว่า
  Login ด้วยวิธีไหนก็ตาม — ถ้าต้องการให้ "สมัครสมาชิกแบบ Guest แบบออฟไลน์เต็ม
  รูปแบบ" (กรอกฟอร์มออฟไลน์ได้ แล้วค่อยไปขอ memberId ทีหลังตอนมีเน็ต) จะเป็น
  งานเพิ่มเติมอีกก้อนหนึ่งที่กระทบ Flow การกรอกฟอร์ม/การใช้ `memberId` ในทุกจุด
  ที่พึ่งมันอยู่ (เช่น `checkin()` ต้องมี `userId` เสมอ) แจ้งมาได้ถ้าต้องการให้
  ทำต่อในรอบถัดไป
- **`useOfflineSync.ts` -> Step 1 (Login LINE) ใน `syncNow()`** — เรียก
  `loginWithLine()` เฉพาะตอนกำลัง Sync (ซึ่งเช็คแล้วว่าออนไลน์อยู่ก่อนเข้า
  `syncNow()` เสมอ) จึงไม่ขัดกับเป้าหมาย Offline First
- **IndexedDB** — ตรวจสอบแล้วว่าไม่จำเป็นสำหรับแอปนี้ ข้อมูลทั้งหมด (โปรไฟล์,
  สถานะฐานที่ผ่านแล้ว, คิว Sync) มีขนาดเล็กมาก (ไม่กี่ KB) อยู่ใน LocalStorage
  ได้สบาย ๆ ภายในโควต้า 5-10MB ปกติของเบราว์เซอร์ — การเปลี่ยนไปใช้ IndexedDB
  จะเพิ่มความซับซ้อนของโค้ดโดยไม่ได้ประโยชน์เพิ่มจริง จึงคงใช้ LocalStorage
  เหมือนเดิมตามหลัก "คงโครงสร้างเดิมของโปรเจกต์ให้มากที่สุด"
